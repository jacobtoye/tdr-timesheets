import styled from '@emotion/styled';
import CenteredContainer from '../CenteredContainer';

const ListRowIcon = styled(CenteredContainer)`
  margin-right: 8px;
  width: 48px;
  height: 48px;
`;

export default ListRowIcon;
