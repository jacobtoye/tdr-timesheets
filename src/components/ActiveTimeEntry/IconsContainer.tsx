import styled from '@emotion/styled';
import CenteredItemsContainer from './CenteredItemsContainer';

const IconsContainer = styled(CenteredItemsContainer)`
  grid-gap: 32px;
  height: 48px;
  margin-top: 8px;
`;

export default IconsContainer;
