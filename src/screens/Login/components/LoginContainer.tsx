import styled from '@emotion/styled';

import { CenteredContainer } from '../../../components';

export const LoginContainer = styled(CenteredContainer)`
  padding: 2rem;
  width: 50%;
`;

export default LoginContainer;
